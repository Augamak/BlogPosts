import BackButton from "../components/BackButton";

const Contacts = () => {
    return (
        <div className="about">
            <h1>This is Contacts page</h1>
            <BackButton />
        </div>    
    );
};

export default Contacts;